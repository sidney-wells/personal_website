import { Infinity } from '../../src/components';
import { render } from '../../test-utils';

describe('Infinity', () => {
  it('should render an infinity', () => {
    render(<Infinity />);
  });
});
