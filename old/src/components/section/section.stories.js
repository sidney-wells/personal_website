import Section from '.';

const story = {
  title: 'Atoms/Section',
  component: Section
};

export default story;

const Template = args => <Section {...args} />;

export const Default = Template.bind({});
