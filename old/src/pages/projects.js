import { Layout, Heading, Box } from '../components';

const Projects = () => (
  <>
    <Layout>
      <Box sx={{ textAlign: 'center' }}>
        <Heading pb="s" as="h1">
          projects page
        </Heading>
      </Box>
    </Layout>
  </>
);

export default Projects;
